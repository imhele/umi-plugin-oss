import OSS from 'ali-oss';

export default class SyncFiles {
  constructor({
    accessKeyId,
    accessKeySecret,
    stsToken,
    secure,
    timeout,
    bucket,
    acl,
    headers,
  }) { }
};
